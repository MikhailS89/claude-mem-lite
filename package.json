{
  "name": "claude-mem-lite",
  "version": "0.6.3",
  "description": "Local, offline, auditable session memory for Claude Code. Zero dependencies.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "test": "node --no-warnings --test \"test/*.test.mjs\"",
    "search": "node --no-warnings scripts/search.mjs"
  },
  "license": "MIT"
}
